
const ChannelsReducer = (state = {}, action) => {
  Object.freeze(state);
  switch (action.type) {
    case 'placeholder':

      break;
    default:
      return state;
  }
};

export default ChannelsReducer;
